TASK: Form Assembly + PDF Download
FILES: taxfile/src/modules/forms-generation/assembler.ts
DEPENDS ON: 0042,0043,0044,0045,0046,0047
COMPLEXITY: medium

DESCRIPTION:
assembleForms(taxReturn): FormPDF[]. Conditionally include schedules. Render each as PDF blob via @react-pdf/renderer.

APPROACH:
Conditional inclusion based on return data. pdf() to render to blob.

OUTCOME:
Form assembler producing all needed PDFs.

ACCEPTANCE CRITERIA:
- Conditional Schedule A (itemizing)
- Conditional Schedule D (cap gains)
- Returns {formName, pdfBuffer}[]
- Under 300 lines

- Created assembler.tsx (108 lines) with assembleForms + downloadForms
- FormPDF interface: { formName, pdfBlob: Blob }
- Conditional inclusion: hasData() for schedules with entries, hasNonZeroValues() for calculated schedules
- Schedule A: conditional (itemizing), Schedule B: conditional (payer data), Schedule D: conditional (cap gains)
- Schedule 8812: conditional (CTC non-zero), Schedules 1/2/3: conditional (non-zero values)
- NJ schedules: all conditional on data presence
- downloadForms: creates object URLs and triggers browser downloads
- TypeScript compiles clean

ACCEPTED: 2026-03-26 -- created and verified
DECISIONS LOG:
