TASK: Upload Wizard Step
FILES: taxfile/src/app/return/page.tsx, taxfile/src/modules/user-flow/steps/upload-step.tsx
DEPENDS ON: 0049
COMPLEXITY: medium

DESCRIPTION:
Drag-drop upload, document type selector, progress indicator, uploaded doc list with badges, delete, mobile camera capture, trigger OCR extraction, show confidence.

APPROACH:
shadcn Card/Button/Badge/Progress. HTML5 drag events. Camera via input capture. API call for OCR.

OUTCOME:
Upload step with OCR extraction.

ACCEPTANCE CRITERIA:
- Drag-drop works
- Type selectable per file
- Progress shown
- Doc list with badges
- Mobile camera
- OCR via API
- Under 300 lines per file

- Created upload-step.tsx (213 lines) with drag-drop, type selector, progress, doc list, camera
- Created src/app/return/page.tsx (24 lines) as wizard router
- Drag-drop via onDragOver/onDragLeave/onDrop handlers
- Document type selectable per file via Select component, detectTypeFromName() for auto-detection
- Progress bar during OCR extraction, calls /api/documents/extract
- Doc list with Badge for type and confidence %, remove button
- Mobile camera via input capture=environment
- Uses ExtractedDocument.sourceFile (not fileName), reviewed: false
- TypeScript compiles clean

ACCEPTED: 2026-03-26 -- created and verified
DECISIONS LOG:
