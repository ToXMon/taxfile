TASK: Income Aggregation — W-2 Wages + 1099 Income
FILES: taxfile/src/modules/tax-engine/federal/income-aggregation.ts
DEPENDS ON: 0021
COMPLEXITY: medium

DESCRIPTION:
Aggregate: W-2 box 1 wages, 1099-INT box 1 interest, 1099-DIV 1a ordinary + 1b qualified dividends, 1099-NEC box 1, 1099-MISC box 3 other + box 1 rents. Return IncomeSummary with per-source breakdown. Each value is TaxLineItem with source tracing.

APPROACH:
aggregateIncome(documents): IncomeSummary. Filter by type, sum relevant boxes. Source tracing on every value.

OUTCOME:
Income aggregation from all documents.

ACCEPTANCE CRITERIA:
- Aggregates W-2, 1099-INT, 1099-DIV, 1099-NEC, 1099-MISC
- Every value has source tracing
- Returns typed IncomeSummary
- Under 300 lines

DECISIONS LOG:
- Created module types.ts first (IncomeSummary, CalculationContext, DeductionResult, FederalTaxResult, NJTaxResult)
- sumField() helper eliminates duplication across 9 income source aggregations
- Flagged mapper fields propagated as TaxCalculationFlag with needsHumanReview=true
- Gross income excludes capital gains (they go through Schedule D separately, not added to Line 1-8 total)
- 1099-B uses proceeds (boxes 2/4) not gains — gains require cost basis subtraction in Schedule D logic
- Qualified dividends use formLine 'QD' (not a real 1040 line) since they go to a worksheet, not directly on 1040
- EMPTY_LINE fallback for zero-value items keeps type consistency
- 148 lines, well under 300 limit
- ACCEPTED: all criteria met
DECISIONS LOG:
