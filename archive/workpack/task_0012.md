TASK: Client-Side OCR Wrapper — Tesseract.js
FILES: taxfile/src/modules/document-extraction/ocr/client-ocr.ts
DEPENDS ON: 0011
COMPLEXITY: medium

DESCRIPTION:
Client-side OCR using Tesseract.js. extractTextFromImage(file: File): Promise<OCRResult> with raw text, confidence, word-level bounding boxes. Handle PDF via pdfjs-dist. Multi-page support.

APPROACH:
Wrap Tesseract.js createWorker. PDF to canvas via pdfjs-dist. Return structured per-page result.

OUTCOME:
Client-side OCR for PDF/image files.

ACCEPTANCE CRITERIA:
- Accepts File input (PDF or image)
- Returns OCRResult with text, confidence, pages
- Handles multi-page PDFs
- Typed, no any
- Under 300 lines

DECISIONS LOG:
- Tesseract.js worker loaded dynamically via import() to avoid SSR issues in Next.js
- Worker is singleton — reused across multiple extractions in same session, with terminateOCRWorker() for cleanup
- pdfjs-dist workerSrc set to empty string — lets pdfjs-dist handle worker loading internally
- PDF rendered at 2x scale for better OCR accuracy
- Tesseract confidence converted from 0-100 to 0-1 range for consistency with module types
- Used unknown + explicit cast pattern for Tesseract worker API instead of any
- Created module-internal types.ts (task not explicitly listed but needed as dependency per AGENTS.md)
- 152 lines, well under 300 limit
- ACCEPTED: all criteria met
DECISIONS LOG:
