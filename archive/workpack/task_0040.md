TASK: Tax Engine Orchestrator — Full Pipeline
FILES: taxfile/src/modules/tax-engine/orchestrator.ts
DEPENDS ON: 0028,0029,0030,0031,0032,0037,0038,0039
COMPLEXITY: large

DESCRIPTION:
Full pipeline: income agg -> Sch B -> Sch 1 -> Sch D -> deduction resolver -> Sch A -> 1040 core -> Sch 2 -> Sch 3 -> CTC/Sch 8812 -> EITC -> education -> federal total -> NJ income -> NJ Sch B -> NJ Sch A -> NJ Sch C -> NJ-1040 -> NJ property tax -> NJ EITC -> NJ total -> CompleteTaxReturn with audit trail.

APPROACH:
Orchestration function calling all sub-modules in order. Collect TaxLineItems for audit trail. Assemble CompleteTaxReturn.

OUTCOME:
Complete tax calculation pipeline.

ACCEPTANCE CRITERIA:
- Full federal + NJ pipeline
- Returns CompleteTaxReturn per scope.md
- Audit trail on every value
- Handles reference scenario
- Under 300 lines

DECISIONS LOG:
- Full pipeline: 20 steps chaining all sub-modules in correct dependency order
- Federal: income agg → Sch B → Sch D → Sch 1 → deduction → Sch A → 1040 core → Sch 2 → Sch 3 → CTC/8812 → EITC → education
- NJ: income agg → Sch B → Sch C → Sch A → property tax election → EITC → 1040 core
- Config imported directly (FEDERAL_TAX_2025, NJ_TAX_2025, CREDITS_2025) — orchestrator is the composition root
- Sub-modules use dependency injection; orchestrator is the only place that imports config directly
- Audit trail entries on key calculation steps (gross income, AGI, tax, credits)
- All flags collected into single array for CompleteTaxReturn
- Also created: federal/index.ts (26 lines), nj/index.ts (20 lines), updated config index with CREDITS_2025 export
- DeductionResult.flags needed optional chaining (deduction.flags ?? []) — existing code doesn't guarantee flags array
- Schedule A fallback uses {} as never when not itemized (cleaner than null checks throughout)
- 206 lines, well under 300 limit

ACCEPTED: all criteria met
