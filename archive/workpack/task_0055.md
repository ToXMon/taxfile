TASK: API Routes — Extract + Documents CRUD
FILES: taxfile/src/app/api/extract/route.ts, taxfile/src/app/api/documents/route.ts
DEPENDS ON: 0020,0004
COMPLEXITY: medium

DESCRIPTION:
POST /api/extract: FormData -> ExtractedDocument. CRUD /api/documents: GET/POST/PUT/DELETE. Zod validation.

APPROACH:
Next.js Route Handlers. Server OCR fallback. DB interface. Zod schemas.

OUTCOME:
Working extract and documents API.

ACCEPTANCE CRITERIA:
- POST /api/extract returns ExtractedDocument
- Full CRUD on /api/documents
- Zod validation
- Error handling
- Under 300 lines per file

- Created api/documents/extract/route.ts (52 lines): POST accepts FormData (file + documentType), runs extractDocument pipeline, returns ExtractedDocument JSON
- Created api/documents/route.ts (114 lines): Full CRUD with in-memory Map store
  - GET: lists all documents with count
  - POST: validates with Zod (taxLineItemSchema with proper TaxLineItemTrace), creates document
  - PUT: validates updates, merges with existing document
  - DELETE: removes by id query parameter
- Zod schemas match exact TaxLineItem structure (trace as required object with optional fields)
- Error handling: 400 for validation, 404 for not found, 500 for extraction failures
- Note: placed at /api/documents/extract to match upload-step.tsx call path
- TypeScript compiles clean

ACCEPTED: 2026-03-26 -- created and verified
DECISIONS LOG:
