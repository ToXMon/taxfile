TASK: Root AGENTS.md + HARNESS.md + Directory Structure
FILES: taxfile/AGENTS.md, taxfile/HARNESS.md, taxfile/data/.gitkeep, taxfile/docs/state/.gitkeep
DEPENDS ON: 0001
COMPLEXITY: small

DESCRIPTION:
Create root AGENTS.md with repo overview, tech stack, module map, global standards. Copy HARNESS.md. Create data/ and docs/state/ directories.

APPROACH:
AGENTS.md follows harness standard. HARNESS.md is full development harness.

OUTCOME:
Root documentation files exist. data/ and docs/state/ directories created.

ACCEPTANCE CRITERIA:
- AGENTS.md contains repo purpose, tech stack, module map
- HARNESS.md contains full development harness rules
- data/ is in .gitignore

DECISIONS LOG:
- Adapted HARNESS.md from web3-agent-akash template, adding TaxFile-specific rules (tax calculation flagging, PII protection, IRS publication traceability).
- AGENTS.md structured with: purpose, tech stack table, module map table, global standards (type system, file limits, tax rules, PII, AGENTS.md-first, disclaimer).
- Created data/.gitkeep and docs/state/.gitkeep via mkdir -p + touch.
- Appended `/data` to .gitignore (not `/data/` — both work but simpler form).
DECISIONS LOG:
- Completed in prior session before workpack tracking started. Verified: all output files exist.
- ACCEPTED: all criteria met (verified by file existence)
