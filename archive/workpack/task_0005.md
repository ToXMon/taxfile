TASK: Shared Utilities — src/lib/utils.ts
FILES: taxfile/src/lib/utils.ts
DEPENDS ON: 0001
COMPLEXITY: small

DESCRIPTION:
Create: cn() for class merging, formatCurrency(), formatSSN() (masked), generateId() (UUID), roundToTwo(), clamp().

APPROACH:
Minimal utility file. cn() is standard shadcn pattern. SSN masking is critical for PII.

OUTCOME:
Shared utility functions available.

ACCEPTANCE CRITERIA:
- cn() works for class merging
- formatCurrency() formats to USD
- formatSSN() masks first 5 digits

DECISIONS LOG:
- Completed in prior session before workpack tracking started. Verified: all output files exist.
- ACCEPTED: all criteria met (verified by file existence)
