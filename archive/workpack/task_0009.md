TASK: ESLint Config — 300-line, 20-file, No-Any Rules
FILES: taxfile/.eslintrc.json
DEPENDS ON: 0001
COMPLEXITY: small

DESCRIPTION:
Configure ESLint: max file 300 lines (error), no @typescript-eslint/no-explicit-any (error). Extend Next.js and TypeScript recommended configs.

APPROACH:
Extend next/core-web-vitals. Add max-lines rule. Add no-any rule. 20-file directory check as npm script.

OUTCOME:
ESLint catches file length and any type violations.

ACCEPTANCE CRITERIA:
- no-explicit-any set to error
- File length limit 300 lines enforced
- npm run lint catches violations

DECISIONS LOG:
- max-lines set to 300 with skipBlankLines and skipComments — effective limit is on code lines only
- @typescript-eslint/no-explicit-any set to error (not warn) per rules.md
- 20-file directory check implemented as shell one-liner in npm script rather than ESLint plugin — simpler, no dependency needed
- dir-check script uses find+sed+awk pipeline, exits with code 1 and prints error if any dir exceeds 20 files
- ACCEPTED: all criteria met
DECISIONS LOG:
