TASK: API Routes — Calculate + Forms
FILES: taxfile/src/app/api/calculate/route.ts, taxfile/src/app/api/forms/route.ts
DEPENDS ON: 0040,0048
COMPLEXITY: medium

DESCRIPTION:
POST /api/calculate: TaxCalculationInputs -> CompleteTaxReturn. POST /api/forms: CompleteTaxReturn -> PDF array (base64). Zod validation.

APPROACH:
Calculate calls orchestrator. Forms calls assembler, encodes PDFs as base64.

OUTCOME:
Working calculate and forms API.

ACCEPTANCE CRITERIA:
- POST /api/calculate returns CompleteTaxReturn
- POST /api/forms returns {formName, pdfBase64}[]
- Zod validation
- Under 300 lines per file

- Created api/calculate/route.ts (107 lines): POST accepts OrchestratorInput with full Zod validation (TaxpayerInfo with Dependent fields, ExtractedDocument[], AdditionalAnswers), calls calculateFullReturn, returns CompleteTaxReturn JSON
- Created api/forms/route.ts (89 lines): POST accepts full CompleteTaxReturn with summary/auditTrail/flags, calls assembleForms, converts Blobs to base64 via FileReader, returns {formName, pdfBase64}[]
- Fixed Dependent schema: added relationship, birthYear, qualifyingRelative to match type
- Fixed forms schema: added full taxpayer (with address/dependents), summary, auditTrail, flags sections
- Error handling: 400 validation, 500 calculation/generation failures
- TypeScript compiles clean

ACCEPTED: 2026-03-26 -- created and verified
DECISIONS LOG:
