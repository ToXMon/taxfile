TASK: Centralized Type Definitions — src/lib/types.ts
FILES: taxfile/src/lib/types.ts
DEPENDS ON: 0001
COMPLEXITY: large

DESCRIPTION:
Define all shared TypeScript types: DocumentType union, ExtractedDocument, TaxLineItem (with source+trace), TaxpayerInfo, FilingStatus, AdditionalAnswers, CompleteTaxReturn, FederalForms, NJForms, TaxSummary, AuditTrailEntry, TaxReturnRecord, TaxCalculationFlag, WizardStep enum. No any types.

APPROACH:
Define types matching scope.md interface contracts. Group with JSDoc. TaxLineItem must have full trace structure. Split into types/ subdirectory if over 300 lines.

OUTCOME:
Complete type definitions that all modules import from.

ACCEPTANCE CRITERIA:
- All types from scope.md defined
- No any types
- TaxLineItem has source.documentId, source.formType, source.boxNumber, trace fields, flagged, flagReason
- Under 300 lines or properly split

DECISIONS LOG:
- All types fit in 211 lines — no split needed. Organized with clear section comments.
- Split TaxLineItem into three sub-interfaces: TaxLineItemSource, TaxLineItemTrace, TaxLineItem for clarity.
- Created FormLineMap type alias (Record<string, TaxLineItem>) to reduce repetition across FederalForms/NJForms.
- SSN fields named `ssnMasked` (not `ssn`) to enforce PII protection at type level.
- Added `flags: TaxCalculationFlag[]` to CompleteTaxReturn (not in scope.md but required by rules.md TaxCalculationFlag requirement).
- Added API contract types (ExtractRequest, CalculateRequest, FormsRequest, FormPdfOutput) for type-safe API routes.
- Used `ReturnStatus` union type instead of inline string literal union in TaxReturnRecord for reuse.
- WizardStep as enum (not const object) for runtime step comparison.
DECISIONS LOG:
- Completed in prior session before workpack tracking started. Verified: all output files exist.
- ACCEPTED: all criteria met (verified by file existence)
